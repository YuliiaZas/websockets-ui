// import { rooms } from '../../index.js';

export default function updateRoom(ws) {
    // const roomId = rooms.length;
    // console.log('rooms');
    // let room = { type: 'create_room', data: "", id: 0 };
    
    // console.log(rooms);
    // const newRoom = { roomId, roomUsers: [] };
    // rooms.push(newRoom);
    // ws.send(JSON.stringify({ type: 'update_room', data: rooms, id: 0 }));
}